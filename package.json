{
  "name": "aurora",
  "version": "0.0.0",
  "description": "Toast library for React with no dependencies and fully customizable",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo build --cache-dir=.turbo",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:open": "npm run test:open --workspace @madeval/aurora"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "turbo": "1.11.2"
  }
}
