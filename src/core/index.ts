import { eventCreate } from './eventCreate'

const { success, info, warning, error, promise, custom } = eventCreate

export const toast = {
  success,
  info,
  warning,
  error,
  promise,
  custom,
}
