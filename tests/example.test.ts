import { describe, it, expect } from 'vitest'
import {} from '@testing-library/react'

describe('🚨 This is an example! [Remove then]', () => {
  it('simple test', () => {
    expect(1).toBe(1)
  })
})
